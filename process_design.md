**Goal**
Divide the business process into clear **processes**, **sub-processes**, and **swimlanes** based on the provided service documentation.

**Context**
The service document is titled:
**إصدار شهادة تسجيل سفينة أو وحدة بحرية مؤقتة** (Temporary Registration Certificate for a Vessel or Marine Unit).

**Instructions**

1. **Document Analysis**

   * Analyze the DOCX file **إصدار شهادة تسجيل سفينة أو وحدة بحرية مؤقتة**.
   * Convert and analyze the content using **Markdown MCP tools**.
   * Identify business steps, actors, decisions, and dependencies.

2. **Context Completion**

   * Detect and load any **additional files or documents** required to fully understand the business flow (e.g., regulations, prerequisites, related services).
   * Clearly state any assumptions if required documents are missing.

3. **Process Design**

   * Recommend the **end-to-end business process** required to fulfill the service.
   * Decompose the process into:

     * Main processes
     * Sub-processes
   * Assign each step to appropriate **swimlanes** (e.g., Applicant, System, Authority, External Entity).

4. **Output Requirements**

   * Present the result in a **clear, structured format**, including:

     * Process hierarchy (Process → Sub-process → Steps)
     * Swimlane mapping for each step
     * Logical flow with correct sequencing (no skipped or unordered steps)
   * Use concise, business-friendly language suitable for:

     * BPMN modeling
     * System design
     * Automation or digital transformation initiatives

**Expected Outcome**
A well-structured business process model that can be directly used for:

* Swimlane diagrams
* BPMN workflows
* Service digitalization and automation
